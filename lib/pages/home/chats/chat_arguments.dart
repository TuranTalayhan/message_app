class ChatArguments {
  final String groupName;
  final String? groupPicture;
  final String lastMessage;
  final List members;

  ChatArguments(
      this.groupName, this.groupPicture, this.lastMessage, this.members);
}
